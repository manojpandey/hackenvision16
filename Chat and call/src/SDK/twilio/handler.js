/*!
      File Description: Handles routes for Google SDK
*/

"use strict";

import {
  IReply,
  Request,
} from 'hapi';

import controller from './controller';

const handler = {

  /*
      Add handler Objects as follows

      splash( request: Request, reply: IReply ) {

      controller.*function name*( params )

    }
  */

  getPicture( Request, IReply ) {
    
  }

}
